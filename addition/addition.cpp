#include <iostream>
using namespace std;

int main() {
    long long a, b;  // Use long long to handle large numbers up to 10^18
    cin >> a >> b;   // Read input
    cout << a + b << endl;  // Output the sum
    return 0;
}
